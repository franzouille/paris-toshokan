# Paris Bibliothèques Launcher

Static single-page launcher to open filtered searches on bibliotheques.paris.fr.

## Usage
- Open `index.html` in Safari / Chrome.
- Type a query, pick a preset, pick types, open search.

## iOS
- Put `index.html` in iCloud Drive.
- Open it in Safari.
- Share → Add to Home Screen.

